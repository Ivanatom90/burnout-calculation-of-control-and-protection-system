#pragma once
#include <string>
#include <string_view>
#include <vector>
#include <algorithm>

using namespace std;

vector<string> ReadlineS(const string& str);
vector<double> ReadlineNum(const string& str);
